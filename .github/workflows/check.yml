name: Lint
on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@ee0669bd1cc54295c223e0bb666b733df41de1c5 # v2
        with:
          submodules: 'recursive'
          persist-credentials: false
      - name: Reconfigure git to use HTTP authentication
        run: >
          git config --global url."https://github.com/".insteadOf
          ssh://git@github.com/
      - name: Use Node.js 20.x
        uses: actions/setup-node@f1f314fca9dfce2769ece7d933488f076716723e # v1
        with:
          node-version: 20.x
      - run: npm ci
      - run: npm run lint
      - run: npm run format:check
      - run: npx tsc --noEmit ably.d.ts modular.d.ts
      # Type-checks the wrapper packages' sources and their hand-written declaration files
      # together, against the core resolved through the tsconfig's `paths`. The React hooks'
      # declaration files are generated rather than hand-written, so the `/react` subpath can only
      # be checked against them once they exist — and checking against the generated types is the
      # right thing to do anyway, since they are what a consumer resolves.
      - run: npm run build:react
      - run: npm run check:packages
      # for some reason, this doesn't work in CI using `npx attw --pack .`
      - run: npm pack
      - run: npx attw ably-$(node -e "console.log(require('./package.json').version)").tgz --summary --exclude-entrypoints 'ably/modular'
      # see https://github.com/ably/ably-js/issues/1546 for why we ignore 'false-cjs' currently.
      # should remove when switched to auto-generated type declaration files for modular variant of the library.
      - run: npx attw ably-$(node -e "console.log(require('./package.json').version)").tgz --summary --entrypoints 'ably/modular' --ignore-rules false-cjs
      # Built explicitly rather than relying on the root `npm pack` above having run
      # `prepare`, so that packing the wrapper packages does not depend on a side effect of
      # an earlier step. Neither package has a `prepare` of its own to build it.
      - run: npm run build:packages
      - run: npm pack ./packages/pubsub-device ./packages/pubsub-server
      # The `/push` entry point is excluded because it is honestly import-only and attw reports its
      # CJS resolution as failing, which is the truth: the core's `ably/push` declares no `require`
      # condition and its build is reachable by no other exported path, so the only way to obtain
      # the plugin is to import it. Remove this exclusion if the core ever gains that condition.
      - run: npx attw ably-pubsub-device-$(node -e "console.log(require('./packages/pubsub-device/package.json').version)").tgz --summary --exclude-entrypoints '@ably/pubsub-device/push'
      - run: npx attw ably-pubsub-server-$(node -e "console.log(require('./packages/pubsub-server/package.json').version)").tgz --summary
      - run: npm audit --production
