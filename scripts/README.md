These are scripts used by the CI for deployment, etc.


### cdn_deploy.js

Deploys code and source maps to the CDN.
Requires aws-sdk, nodegit and minimist:
```
# npm install --no-save aws-sdk nodegit minimist
```
