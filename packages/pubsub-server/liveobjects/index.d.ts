/**
 * The full public type surface of the core's LiveObjects plugin. Safe as a star re-export because
 * it resolves at compile time only — see ../../shared/core-exports.ts for why the *runtime*
 * re-export is enumerated instead.
 */
export * from 'ably/liveobjects';
